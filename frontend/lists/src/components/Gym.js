import React from 'react';

export default function Gym() {
    const gymStyle = {
        height: "100%",
        display: "flex",
        flexDirection: "column",
        justifyContent: "center",
        alignItems: "center"
    };
    return (
        <div style={gymStyle}>
            Hello Gym
        </div>
    )
}
